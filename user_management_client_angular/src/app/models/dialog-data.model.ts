/* Defining the interface for the data that will be passed to the dialog. */
export interface DialogData {
  cancelButtonLabel?: string;
  confirmButtonLabel?: string;
  dialogHeader?: string;
  dialogContent?: any;
  dialogSecondContent?: string;
}
