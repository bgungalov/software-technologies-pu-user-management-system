import { TableColumn } from 'src/app/models/table-column.model';

/* ClickableCell is a class that has a value and a column property. */
export class ClickableCell {
  value: any;
  column: TableColumn;
}
